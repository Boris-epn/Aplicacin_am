<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:name=".VitusApp"
        android:supportsRtl="true"
        android:theme="@style/Theme.Interfaces">
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="Vitus"
            android:parentActivityName=".LoginActivity" />
        <activity
            android:name=".ConfirmationActivity"
            android:exported="false"
            android:label="Confirmación"
            android:parentActivityName=".SlotSelectionActivity" />
        <activity
            android:name=".SlotSelectionActivity"
            android:exported="false"
            android:label="Horarios"
            android:parentActivityName=".DoctorSelectionActivity" />
        <activity
            android:name=".DoctorSelectionActivity"
            android:exported="false"
            android:label="Médicos"
            android:parentActivityName=".SpecialtySelectionActivity" />
        <activity
            android:name=".SpecialtySelectionActivity"
            android:exported="false"
            android:label="Especialidades"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Interfaces"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:label="Home"
            android:parentActivityName=".LoginActivity"
            android:theme="@style/Theme.MisActivities"
            android:windowSoftInputMode="adjustResize" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
